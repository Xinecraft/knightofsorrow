language: php

php:
  - 5.5
  - 5.6
  - hhvm

before_script:
  - composer self-update
  - composer install --prefer-source --no-interaction
  - php artisan migrate

script:
  - ./vendor/bin/phpunit

matrix:
  allow_failures:
    - php: hhvm
  fast_finish: true